class PageSpecifications {
  pageMargin = {
    top: 450.51,
    right: 450.51,
    bottom: 450.51,
    left: 450.51,
  };

  font = "Arial";

  fontSize = {
    minContentSize: 8,
    maxContentSize: 11,
    minTitleSize: 9,
    maxTitleSize: 11.5,
    contactInfoSize: 8.5,
    userNameSize: 14,
  };

  tableColumnWidth = {
    left: 70,
    right: 30,
  };

  lineSpacing = 1000;
}

export default new PageSpecifications();
