class PageSpecifications {
  pageMargin = {
    top: 450.51,
    right: 450.51,
    bottom: 450.51,
    left: 450.51,
  };

  font = "Calibri";

  fontSize = {
    minContentSize: 8,
    maxContentSize: 10,
    minTitleSize: 9,
    maxTitleSize: 10.5,
    contactInfoSize: 8,
    userNameSize: 12,
  };

  tableColumnWidth = {
    left: 60,
    right: 40,
  };

  lineSpacing = 1000;
}

export default new PageSpecifications();
